<div class="room">

  <%= link_to rooms_path(id: user.id) , remote: true, method: "post" do %>
    <%= image_tag(user.image, class:"small-avatar") %> <span> <%= user.name %></span>
  <% end %>

</div>
