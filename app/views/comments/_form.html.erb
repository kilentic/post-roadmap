<%= form_for(comment,  remote: true) do |f| %>
  <div class="error-wraper"></div>
  <%= f.text_area :content, autofocus: true, class: "form-control", value: comment.content %>
  <div class="btn-wrap">
    <%= f.submit "Update", class:"btn btn-primary" %>
    <button class="btn bg-yell">Cancer</button>
  </div>
<% end %>
